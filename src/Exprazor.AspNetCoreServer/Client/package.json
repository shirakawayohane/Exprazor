{
  "name": "exprazor-client",
  "version": "0.1.0",
  "description": "Exprazor js client",
  "main": "index.ts",
  "scripts": {
    "dev": "esbuild ./src/index.ts --watch --define:__DEV__=true --outfile=../wwwroot/exprazor.server.js",
    "build": "(echo HELLO && set NODE_ENV=production || NODE_ENV=production) && esbuild ./src/index.ts --minify --bundle --outfile=../wwwroot/exprazor.server.js"
  },
  "author": "WiZLite <black_rais0n.detre@icloud.com>",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.1",
    "glob": "^7.2.0",
    "ts-node": "^10.4.0"
  },
  "dependencies": {
    "@msgpack/msgpack": "^2.7.1",
    "msgpack-lite": "^0.1.26"
  }
}
