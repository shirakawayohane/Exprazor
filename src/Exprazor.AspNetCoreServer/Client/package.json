{
  "name": "exprazor-client",
  "version": "0.1.0",
  "description": "Exprazor js client",
  "main": "index.ts",
  "scripts": {
    "dev": "esbuild ./src/index.ts --watch --define:__DEV__=true --sourcemap --bundle --outfile=../wwwroot/_framework/exprazor.server.js",
    "build": "NODE_ENV=production || set NODE_ENV=production && esbuild ./src/index.ts --minify --bundle --outfile=../wwwroot/_framework/exprazor.server.min.js"
  },
  "author": "WiZLite <black_rais0n.detre@icloud.com>",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.1",
    "glob": "^7.2.0",
    "ts-node": "^10.4.0"
  }
}